type User {
    id: ID!
    name: String!
}

# Root
type Query {
    users: [User]
    countUsers: Int!
}

# Root
type Mutation {
    createUser(name: String!): User!
    updateUser(name: String!): User!
    deleteUser(id: ID!): Boolean
}